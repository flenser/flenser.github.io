title: Flenser's TLs
description: A simple web novel site using the Just the Docs theme!
theme: just-the-docs

url: https://flenser.github.io

color_scheme: dark

defaults:
  - scope:
      path: "docs" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
